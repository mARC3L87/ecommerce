import React from 'react';
import './ShippingWrapper.scss';

const ShippingWrapper = ({ children }) => {
  return <div className='shipping-wrapper'>{children}</div>;
};

export default ShippingWrapper;
